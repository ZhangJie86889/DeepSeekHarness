# DeepSeek Harness 便携版 · 使用说明

> 本文档适用于收到 `DeepSeekHarness_便携版.zip` 压缩包的客户。
> 这是一款**免安装、双击即用**的本地 AI 对话工具，已内置运行环境，无需在电脑上安装任何软件。

---

## 一、系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11 **64 位（x64）** |
| 架构 | **不支持 ARM**（如高通骁龙笔记本无法运行） |
| 联网 | 仅需联网调用 AI 接口；程序本身无需下载依赖 |
| 其他 | 无需安装 Node.js、无需管理员权限 |

---

## 二、解压（这一步很关键）

⚠️ **请务必使用 7-Zip / Bandizip / WinRAR 解压。**
不要用 Windows 自带的“右键 → 发送到 → 压缩(zipped)文件夹”解压，否则依赖目录里超长的文件路径会解压失败，导致程序无法启动。

解压后会得到一个文件夹：**`DeepSeekHarness_便携版`**，双击进入即可。

> 建议把解压出的文件夹放在路径较浅的位置（如 `D:\DeepSeekHarness_便携版`），避免目录层级过深。

---

## 三、解压后的文件清单与操作

| 文件 / 文件夹 | 是什么 | 您应该怎么做 |
|---|---|---|
| **`start.bat`** | 程序启动器 | ✅ **唯一需要您操作的文件：双击它启动程序** |
| `node/` | 内置的 Node.js 运行环境 | ⛔ 勿动、勿删除 |
| `node_modules/` | 程序运行所需的依赖库 | ⛔ 勿动、勿删除 |
| `data/` | 您的配置与个性化设置（含聊天壁纸） | ⛔ 一般勿改；**不要清空**，否则会丢失壁纸等设置 |
| `README.txt` | 原版说明文档 | 📖 可阅读参考，勿修改 |
| `package.json` / `package-lock.json` | 程序元数据 | ⛔ 勿动 |
| `install.log` | 安装日志 | 🗑 可忽略 |

**一句话总结：您只需要双击 `start.bat`，其余文件都不用碰。**

---

## 四、启动与使用

1. **双击 `start.bat`**。
   - 会弹出一个黑色命令行窗口，这是程序在运行服务，**请保持它开着，不要关闭**。
2. 稍等几秒，程序会**自动打开浏览器**并跳转到 `http://127.0.0.1:3080`。
   - 若浏览器没有自动打开，请手动在浏览器地址栏输入上述地址。
3. **首次使用需填入您自己的 DeepSeek API Key**：
   - 登录 DeepSeek 开放平台获取 API Key；
   - 在页面提示处粘贴并保存，即可开始对话。
   - 🔑 注意：每个使用者需要**各自的** API Key，本工具不附带密钥。
4. **聊天界面默认带动态视频壁纸**：
   - 按键盘 **`V`** 键，可在「动态壁纸 / 静态背景」之间切换。

---

## 五、关闭程序

- 关闭浏览器标签页即可停止对话。
- 要**完全退出**，请关闭那个黑色命令行窗口（或在窗口内按 `Ctrl + C`）。

---

## 六、常见问题

**Q1：启动时报错 `EADDRINUSE 127.0.0.1:3080`（端口被占用）？**
上一次没有正常关闭，导致端口仍被占用。请关闭之前的命令行窗口，或在任务管理器中结束占用 3080 端口的进程，然后重新双击 `start.bat`。

**Q2：解压后部分文件报错 / 程序打不开？**
几乎都是因为用了 Windows 自带解压工具导致的长路径问题。请用 7-Zip 重新解压一次即可。

**Q3：想恢复出厂设置（清空我的聊天记录等）？**
关闭程序后，删除 `data/` 目录里的 `sessions`、`storages` 等文件夹即可（⚠️ 这会同时清空聊天记录和壁纸设置，请按需操作）。

---

## 七、给 IT / 管理员的备注

- 本工具为**纯绿色便携版**，所有个人数据都保存在 `data/` 目录内，可整目录拷贝迁移。
- 不需要安装、不写注册表；若杀毒软件误报，可将解压后的文件夹加入白名单。
- 仅支持 64 位 Windows；在 ARM 设备（如骁龙笔记本）上无法运行。

---

如有其他问题，请联系发送本压缩包的人员。
